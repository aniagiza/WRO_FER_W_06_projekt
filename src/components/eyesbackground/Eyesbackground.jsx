import React from "react";
import styles from "./Eyesbackground.scss";

export default () => (
  <div className={ styles.eyesbackground}>

    <div className={styles.landWrapper}>
      <p className={ styles.land}> and landed in my eyes </p>
    </div>
  </div>
)
;
